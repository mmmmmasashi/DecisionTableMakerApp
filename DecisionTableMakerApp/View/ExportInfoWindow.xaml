<Window x:Class="DecisionTableMakerApp.View.ExportInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Excel出力" Height="200" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- タイトル -->
        <TextBlock Text="タイトル:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="TitleTextBox" Text="{Binding TitleText.Value}" Grid.Column="1" VerticalContentAlignment="Center" Margin="0,0,0,10"/>


        <!-- 検査観点 -->
        <TextBlock Grid.Row="1" Text="検査観点:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="1"  x:Name="InspectionTextBox" Text="{Binding InspectionText.Value}" Grid.Column="1" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <!-- 作成者 -->
        <TextBlock Text="作成者:" Grid.Row="2" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox x:Name="AuthorTextBox" Text="{Binding AuthorText.Value}" Grid.Row="2" Grid.Column="1" VerticalContentAlignment="Center" Margin="0,0,0,10"/>

        <!-- ボタン -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Height="32" Content="キャンセル" Width="64" Margin="5,0,0,0" Command="{Binding CancelCommand}"/>
            <Button Height="32" Content="OK" Width="64" Margin="5,0,0,0" Command="{Binding OKCommand}"/>
        </StackPanel>
    </Grid>
</Window>